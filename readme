this project is arm to build a component world that everyone can use the component to build their project, no need to coding for the detail.
every thing is component,every component has a interface,we connect the component with interface, then build our thing(app/website) .
